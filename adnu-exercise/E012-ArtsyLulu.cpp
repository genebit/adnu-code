#include <iostream>
#include <vector>

using namespace std;

int main() {
   
    const int size = 3;
    char characters[size];
    
    for (int i = 0; i < 3; ++i)
    {
        // cin >> characters[size];
        cout << "asd";
    }
    for (char i : characters) 
        cout << i << " ";

    // vector<string> list;
    // for (int i = 0; i < size; i++) {
    //     for (int j = 0; j <= i; j++) {

    //     }    
    // }
    
    return 0;
}